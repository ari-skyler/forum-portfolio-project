<div class="post">
<h1>New Post</h1>
<form id="post" action="/posts" method="post">
  <input type="text" name="raw_title" placeholder="Post Title">
  <input name="delta" type="hidden">
      <div id="editor">
      </div>
  <input class="green" type="submit" value="Post">
</form>
<a href="/posts"><button class="form-button red">Cancel</button></a><br><br><br>
</div>
<script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
<script>
  var toolbarOptions = [[{ 'header': [2, 3, false] }], ['bold', 'italic', 'underline'], ['link'], [{'list': 'bullet'}]]
  var quill = new Quill('#editor', {
    placeholder: "Post Content",
    theme: 'snow',
    modules: {toolbar: toolbarOptions}
  });
  var form = document.querySelector('#post');
  form.onsubmit = function() {
  // Populate hidden form on submit
  var content = document.querySelector('input[name=delta]');
  content.value = JSON.stringify(quill.getContents());
  return true;
};
</script>
